# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Security (FastAPI)
SECRET_KEY=change-this-to-a-secure-random-string-in-production

# Database (PostgreSQL)
POSTGRES_PASSWORD=change-this-to-a-secure-password

# Service URLs (for multi-instance deployment)
# Leave commented for single-server deployment
# BACKEND_URL=http://10.0.1.2:8000
# AI_SERVICE_URL=http://10.0.1.3:11434

# React Frontend (for build time)
VITE_API_URL=/api

# Legacy Node.js Configuration (kept for backward compatibility)
PORT=3000
NODE_ENV=development
DATABASE_PATH=./data/workspaces.db
SYNC_SCHEDULE=0 */6 * * *
